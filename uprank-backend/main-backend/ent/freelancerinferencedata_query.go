// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/notzree/uprank-backend/main-backend/ent/freelancerinferencedata"
	"github.com/notzree/uprank-backend/main-backend/ent/predicate"
	"github.com/notzree/uprank-backend/main-backend/ent/upworkfreelancer"
)

// FreelancerInferenceDataQuery is the builder for querying FreelancerInferenceData entities.
type FreelancerInferenceDataQuery struct {
	config
	ctx                  *QueryContext
	order                []freelancerinferencedata.OrderOption
	inters               []Interceptor
	predicates           []predicate.FreelancerInferenceData
	withUpworkfreelancer *UpworkFreelancerQuery
	withFKs              bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the FreelancerInferenceDataQuery builder.
func (fidq *FreelancerInferenceDataQuery) Where(ps ...predicate.FreelancerInferenceData) *FreelancerInferenceDataQuery {
	fidq.predicates = append(fidq.predicates, ps...)
	return fidq
}

// Limit the number of records to be returned by this query.
func (fidq *FreelancerInferenceDataQuery) Limit(limit int) *FreelancerInferenceDataQuery {
	fidq.ctx.Limit = &limit
	return fidq
}

// Offset to start from.
func (fidq *FreelancerInferenceDataQuery) Offset(offset int) *FreelancerInferenceDataQuery {
	fidq.ctx.Offset = &offset
	return fidq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (fidq *FreelancerInferenceDataQuery) Unique(unique bool) *FreelancerInferenceDataQuery {
	fidq.ctx.Unique = &unique
	return fidq
}

// Order specifies how the records should be ordered.
func (fidq *FreelancerInferenceDataQuery) Order(o ...freelancerinferencedata.OrderOption) *FreelancerInferenceDataQuery {
	fidq.order = append(fidq.order, o...)
	return fidq
}

// QueryUpworkfreelancer chains the current query on the "upworkfreelancer" edge.
func (fidq *FreelancerInferenceDataQuery) QueryUpworkfreelancer() *UpworkFreelancerQuery {
	query := (&UpworkFreelancerClient{config: fidq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := fidq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := fidq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(freelancerinferencedata.Table, freelancerinferencedata.FieldID, selector),
			sqlgraph.To(upworkfreelancer.Table, upworkfreelancer.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, freelancerinferencedata.UpworkfreelancerTable, freelancerinferencedata.UpworkfreelancerColumn),
		)
		fromU = sqlgraph.SetNeighbors(fidq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first FreelancerInferenceData entity from the query.
// Returns a *NotFoundError when no FreelancerInferenceData was found.
func (fidq *FreelancerInferenceDataQuery) First(ctx context.Context) (*FreelancerInferenceData, error) {
	nodes, err := fidq.Limit(1).All(setContextOp(ctx, fidq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{freelancerinferencedata.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (fidq *FreelancerInferenceDataQuery) FirstX(ctx context.Context) *FreelancerInferenceData {
	node, err := fidq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first FreelancerInferenceData ID from the query.
// Returns a *NotFoundError when no FreelancerInferenceData ID was found.
func (fidq *FreelancerInferenceDataQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fidq.Limit(1).IDs(setContextOp(ctx, fidq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{freelancerinferencedata.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (fidq *FreelancerInferenceDataQuery) FirstIDX(ctx context.Context) int {
	id, err := fidq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single FreelancerInferenceData entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one FreelancerInferenceData entity is found.
// Returns a *NotFoundError when no FreelancerInferenceData entities are found.
func (fidq *FreelancerInferenceDataQuery) Only(ctx context.Context) (*FreelancerInferenceData, error) {
	nodes, err := fidq.Limit(2).All(setContextOp(ctx, fidq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{freelancerinferencedata.Label}
	default:
		return nil, &NotSingularError{freelancerinferencedata.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (fidq *FreelancerInferenceDataQuery) OnlyX(ctx context.Context) *FreelancerInferenceData {
	node, err := fidq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only FreelancerInferenceData ID in the query.
// Returns a *NotSingularError when more than one FreelancerInferenceData ID is found.
// Returns a *NotFoundError when no entities are found.
func (fidq *FreelancerInferenceDataQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = fidq.Limit(2).IDs(setContextOp(ctx, fidq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{freelancerinferencedata.Label}
	default:
		err = &NotSingularError{freelancerinferencedata.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (fidq *FreelancerInferenceDataQuery) OnlyIDX(ctx context.Context) int {
	id, err := fidq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of FreelancerInferenceDataSlice.
func (fidq *FreelancerInferenceDataQuery) All(ctx context.Context) ([]*FreelancerInferenceData, error) {
	ctx = setContextOp(ctx, fidq.ctx, "All")
	if err := fidq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*FreelancerInferenceData, *FreelancerInferenceDataQuery]()
	return withInterceptors[[]*FreelancerInferenceData](ctx, fidq, qr, fidq.inters)
}

// AllX is like All, but panics if an error occurs.
func (fidq *FreelancerInferenceDataQuery) AllX(ctx context.Context) []*FreelancerInferenceData {
	nodes, err := fidq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of FreelancerInferenceData IDs.
func (fidq *FreelancerInferenceDataQuery) IDs(ctx context.Context) (ids []int, err error) {
	if fidq.ctx.Unique == nil && fidq.path != nil {
		fidq.Unique(true)
	}
	ctx = setContextOp(ctx, fidq.ctx, "IDs")
	if err = fidq.Select(freelancerinferencedata.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (fidq *FreelancerInferenceDataQuery) IDsX(ctx context.Context) []int {
	ids, err := fidq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (fidq *FreelancerInferenceDataQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, fidq.ctx, "Count")
	if err := fidq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, fidq, querierCount[*FreelancerInferenceDataQuery](), fidq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (fidq *FreelancerInferenceDataQuery) CountX(ctx context.Context) int {
	count, err := fidq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (fidq *FreelancerInferenceDataQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, fidq.ctx, "Exist")
	switch _, err := fidq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (fidq *FreelancerInferenceDataQuery) ExistX(ctx context.Context) bool {
	exist, err := fidq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the FreelancerInferenceDataQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (fidq *FreelancerInferenceDataQuery) Clone() *FreelancerInferenceDataQuery {
	if fidq == nil {
		return nil
	}
	return &FreelancerInferenceDataQuery{
		config:               fidq.config,
		ctx:                  fidq.ctx.Clone(),
		order:                append([]freelancerinferencedata.OrderOption{}, fidq.order...),
		inters:               append([]Interceptor{}, fidq.inters...),
		predicates:           append([]predicate.FreelancerInferenceData{}, fidq.predicates...),
		withUpworkfreelancer: fidq.withUpworkfreelancer.Clone(),
		// clone intermediate query.
		sql:  fidq.sql.Clone(),
		path: fidq.path,
	}
}

// WithUpworkfreelancer tells the query-builder to eager-load the nodes that are connected to
// the "upworkfreelancer" edge. The optional arguments are used to configure the query builder of the edge.
func (fidq *FreelancerInferenceDataQuery) WithUpworkfreelancer(opts ...func(*UpworkFreelancerQuery)) *FreelancerInferenceDataQuery {
	query := (&UpworkFreelancerClient{config: fidq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	fidq.withUpworkfreelancer = query
	return fidq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UprankReccomended bool `json:"uprank_reccomended,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.FreelancerInferenceData.Query().
//		GroupBy(freelancerinferencedata.FieldUprankReccomended).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (fidq *FreelancerInferenceDataQuery) GroupBy(field string, fields ...string) *FreelancerInferenceDataGroupBy {
	fidq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &FreelancerInferenceDataGroupBy{build: fidq}
	grbuild.flds = &fidq.ctx.Fields
	grbuild.label = freelancerinferencedata.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UprankReccomended bool `json:"uprank_reccomended,omitempty"`
//	}
//
//	client.FreelancerInferenceData.Query().
//		Select(freelancerinferencedata.FieldUprankReccomended).
//		Scan(ctx, &v)
func (fidq *FreelancerInferenceDataQuery) Select(fields ...string) *FreelancerInferenceDataSelect {
	fidq.ctx.Fields = append(fidq.ctx.Fields, fields...)
	sbuild := &FreelancerInferenceDataSelect{FreelancerInferenceDataQuery: fidq}
	sbuild.label = freelancerinferencedata.Label
	sbuild.flds, sbuild.scan = &fidq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a FreelancerInferenceDataSelect configured with the given aggregations.
func (fidq *FreelancerInferenceDataQuery) Aggregate(fns ...AggregateFunc) *FreelancerInferenceDataSelect {
	return fidq.Select().Aggregate(fns...)
}

func (fidq *FreelancerInferenceDataQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range fidq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, fidq); err != nil {
				return err
			}
		}
	}
	for _, f := range fidq.ctx.Fields {
		if !freelancerinferencedata.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if fidq.path != nil {
		prev, err := fidq.path(ctx)
		if err != nil {
			return err
		}
		fidq.sql = prev
	}
	return nil
}

func (fidq *FreelancerInferenceDataQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*FreelancerInferenceData, error) {
	var (
		nodes       = []*FreelancerInferenceData{}
		withFKs     = fidq.withFKs
		_spec       = fidq.querySpec()
		loadedTypes = [1]bool{
			fidq.withUpworkfreelancer != nil,
		}
	)
	if fidq.withUpworkfreelancer != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, freelancerinferencedata.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*FreelancerInferenceData).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &FreelancerInferenceData{config: fidq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, fidq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := fidq.withUpworkfreelancer; query != nil {
		if err := fidq.loadUpworkfreelancer(ctx, query, nodes, nil,
			func(n *FreelancerInferenceData, e *UpworkFreelancer) { n.Edges.Upworkfreelancer = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (fidq *FreelancerInferenceDataQuery) loadUpworkfreelancer(ctx context.Context, query *UpworkFreelancerQuery, nodes []*FreelancerInferenceData, init func(*FreelancerInferenceData), assign func(*FreelancerInferenceData, *UpworkFreelancer)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*FreelancerInferenceData)
	for i := range nodes {
		if nodes[i].upwork_freelancer_freelancer_inference_data == nil {
			continue
		}
		fk := *nodes[i].upwork_freelancer_freelancer_inference_data
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(upworkfreelancer.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "upwork_freelancer_freelancer_inference_data" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (fidq *FreelancerInferenceDataQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := fidq.querySpec()
	_spec.Node.Columns = fidq.ctx.Fields
	if len(fidq.ctx.Fields) > 0 {
		_spec.Unique = fidq.ctx.Unique != nil && *fidq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, fidq.driver, _spec)
}

func (fidq *FreelancerInferenceDataQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(freelancerinferencedata.Table, freelancerinferencedata.Columns, sqlgraph.NewFieldSpec(freelancerinferencedata.FieldID, field.TypeInt))
	_spec.From = fidq.sql
	if unique := fidq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if fidq.path != nil {
		_spec.Unique = true
	}
	if fields := fidq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, freelancerinferencedata.FieldID)
		for i := range fields {
			if fields[i] != freelancerinferencedata.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := fidq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := fidq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := fidq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := fidq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (fidq *FreelancerInferenceDataQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(fidq.driver.Dialect())
	t1 := builder.Table(freelancerinferencedata.Table)
	columns := fidq.ctx.Fields
	if len(columns) == 0 {
		columns = freelancerinferencedata.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if fidq.sql != nil {
		selector = fidq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if fidq.ctx.Unique != nil && *fidq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range fidq.predicates {
		p(selector)
	}
	for _, p := range fidq.order {
		p(selector)
	}
	if offset := fidq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := fidq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// FreelancerInferenceDataGroupBy is the group-by builder for FreelancerInferenceData entities.
type FreelancerInferenceDataGroupBy struct {
	selector
	build *FreelancerInferenceDataQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (fidgb *FreelancerInferenceDataGroupBy) Aggregate(fns ...AggregateFunc) *FreelancerInferenceDataGroupBy {
	fidgb.fns = append(fidgb.fns, fns...)
	return fidgb
}

// Scan applies the selector query and scans the result into the given value.
func (fidgb *FreelancerInferenceDataGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fidgb.build.ctx, "GroupBy")
	if err := fidgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FreelancerInferenceDataQuery, *FreelancerInferenceDataGroupBy](ctx, fidgb.build, fidgb, fidgb.build.inters, v)
}

func (fidgb *FreelancerInferenceDataGroupBy) sqlScan(ctx context.Context, root *FreelancerInferenceDataQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(fidgb.fns))
	for _, fn := range fidgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*fidgb.flds)+len(fidgb.fns))
		for _, f := range *fidgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*fidgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fidgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// FreelancerInferenceDataSelect is the builder for selecting fields of FreelancerInferenceData entities.
type FreelancerInferenceDataSelect struct {
	*FreelancerInferenceDataQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (fids *FreelancerInferenceDataSelect) Aggregate(fns ...AggregateFunc) *FreelancerInferenceDataSelect {
	fids.fns = append(fids.fns, fns...)
	return fids
}

// Scan applies the selector query and scans the result into the given value.
func (fids *FreelancerInferenceDataSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, fids.ctx, "Select")
	if err := fids.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*FreelancerInferenceDataQuery, *FreelancerInferenceDataSelect](ctx, fids.FreelancerInferenceDataQuery, fids, fids.inters, v)
}

func (fids *FreelancerInferenceDataSelect) sqlScan(ctx context.Context, root *FreelancerInferenceDataQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(fids.fns))
	for _, fn := range fids.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*fids.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := fids.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
